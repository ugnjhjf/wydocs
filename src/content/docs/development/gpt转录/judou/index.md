---
title: "Judou 架构概览（GPT 转录）"
---

# Judou 架构概览

本文档介绍文言解释器的编译和运行时结构。模组整体目录说明参见上级目录的 `structure.md`。

## 架构图

- `compile.png`：编译流程与主要组件。
- `runtime.png`：运行时流程与主要组件。

## 重构后的命名差异

架构图可能使用旧名称，阅读当前源码时需要注意以下映射：

| 文档里（旧名） | 代码里（新名） |
| --- | --- |
| `frame` | `runtime` |
| `thread` | `runner / PCB` |

这些名称反映的是同一组概念在不同重构阶段的表达。实现时应以当前源码中的类型和接口名称为准。

## 总结

Judou 主要由编译链路和运行时链路组成。架构图中的旧名称需要按上表映射到当前源码。
